# 딥러닝의 정석 with 파이토치 - 실습 환경
# (Windows 11 x64 / Python 3.10 / NVIDIA RTX 5060 Laptop)
#
# 설치:
#   .venv\Scripts\python.exe -m pip install -r requirements-windows.txt
#
# numpy는 1.x로 고정합니다.
#   - gym 이 np.bool8 을 사용 (numpy 2.0 에서 제거됨)
#   - tensorflow 2.16.x 가 numpy<2 를 요구
numpy==1.26.4

# --- 핵심 프레임워크 (CUDA 12.8 빌드) ---
# RTX 50 시리즈(Blackwell, sm_120)는 CUDA 12.8 이상 빌드가 필요합니다.
# PyPI 기본 휠(CPU 전용)로는 GPU 를 쓸 수 없으므로 PyTorch 공식 인덱스를 사용합니다.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
torchsummary

# --- 데이터 / 시각화 ---
matplotlib
seaborn
pandas
scikit-learn
scikit-optimize
yfinance

# --- 영상 처리 (8장, 5장) ---
# PyTorch 인덱스에도 pillow 미러가 있어 버전이 튀지 않도록 고정합니다.
pillow==11.3.0
opencv-python

# --- 오디오 처리 (10장) ---
librosa
soundfile

# --- 사전학습 모델 (7장, 9장, 11장) ---
transformers
timm

# --- 모델 배포 / 최적화 (12장) ---
# onnx 1.18+ 는 ml_dtypes>=0.5 를 요구하는데,
# tensorflow 2.16.2 가 ml_dtypes~=0.3.1 로 고정하므로 충돌합니다.
# onnx 1.17.0 은 ml_dtypes 에 의존하지 않아 두 패키지가 공존 가능합니다.
onnx==1.17.0
onnxruntime

# --- 강화학습 (11장, CartPole-v1) ---
# 노트북이 구(舊) Gym API 를 사용합니다:
#   state = env.reset()                       -> 배열 반환 (0.26+ 는 튜플)
#   next_state, reward, done, _ = env.step(a) -> 4-튜플 (0.26+ 는 5-튜플)
# 0.25.2 가 이 API 의 마지막 버전이므로 고정합니다.
gym==0.25.2
pygame

# --- 비교 예제용 (1장, 2장) ---
# 주의: TensorFlow 는 2.10 이후 네이티브 Windows GPU 를 지원하지 않습니다.
#       Windows 에서는 CPU 전용으로 동작합니다. (비교 예제용이라 문제 없음)
tensorflow==2.16.2

# --- 노트북 실행 환경 ---
jupyterlab
ipykernel
ipywidgets
